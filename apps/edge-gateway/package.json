{
  "name": "@department-voting/edge-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "konsistent": "konsistent",
    "types": "wrangler types",
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "konsistent": "1.0.0-beta.4",
    "wrangler": "4.125.0"
  }
}
